{
    "geode": "5.6.1",
    "gd": {
        "win": "2.2081"
    },
    "id": "buildtogether.editorp2p",
    "name": "EditorP2P",
    "version": "0.2.0",
    "description": "Prototype P2P live collaboration for the Geometry Dash level editor. LAN discovery plus optional bore relay join codes.",
    "developers": ["BuildTogether"],
    "links": {},
    "tags": ["editor", "utility"],
    "settings": {
        "display-name": {
            "name": "Display Name Override",
            "description": "Optional name shown to other players. Leave blank to use your GD username.",
            "type": "string",
            "default": "",
            "match": "^$|^[A-Za-z0-9_\\-]{1,31}$"
        },
        "default-port": {
            "name": "Default Port",
            "description": "TCP port used when hosting a session.",
            "type": "int",
            "default": 43720,
            "min": 1024,
            "max": 65535
        },
        "relay-host": {
            "name": "Relay Host",
            "description": "Bore-compatible relay used for internet join codes. Default bore.pub requires no setup.",
            "type": "string",
            "default": "bore.pub",
            "match": "^[A-Za-z0-9_.\\-]{1,253}$"
        }
    }
}
